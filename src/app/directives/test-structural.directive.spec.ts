import { TestStructuralDirective } from './test-structural.directive';

describe('TestStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new TestStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
